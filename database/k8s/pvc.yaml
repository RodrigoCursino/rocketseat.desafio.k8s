apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-desasio-pvc
  namespace: desafio-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
